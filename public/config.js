// Cấu hình runtime cho ứng dụng (giải pháp dài hạn không cần rebuild khi đổi IP/Domain VPS)
// Để trống ("") để tự động dùng IP/Domain của trình duyệt truy cập và port 5000.
window.VUE_APP_API_GATEWAY_URL = "";
